# 2026-05-30 调试日：树莓派双层架构阶段二

## 背景

根据《架构设计-树莓派上位机.md》，系统升级为“ESP32 舱内飞控 + 树莓派载荷计算机/地面站后端”的双层架构。原则保持不变：树莓派增强但不接管，ESP32 保留传感器、执行器、本地规则和安全护栏。

## 完成事项

### #35 ESP32 UART 主循环接入

新增 `esp32_firmware/uart_link.py` 之后，本次把它真正接进运行链路：

- `config.py.example` 新增 `UART_ENABLED / UART_ID / UART_BAUD / UART_TX_PIN / UART_RX_PIN / UART_OFFLINE_TIMEOUT_MS`
- `main.py` 新增 `_init_uart_link()`，启用后使用 UART2：GPIO17 TX、GPIO16 RX、115200 8N1
- `loop_runtime.py` 新增 `uart_poll` 和 `uart_send_report` 注入点
- 每轮传感器采样后发送 `report`
- 100ms 子循环中持续轮询串口，及时响应树莓派 `ping` 并接收 `advice`
- `state.py` 新增 `pi_online` 和 `pending_pi_decision`

默认 `UART_ENABLED=False`，现有 WiFi-only 比赛路径不受影响。实机双层模式使用：

```python
UART_ENABLED = True
UART_SKIP_WIFI = True
UART_RXBUF = 256
STARTUP_MENU_ON_BOOT = True
```

### #36 Pi advice 下发与 ESP32 护栏

ESP32 现在会把树莓派 `advice` 转换为内部 decision，并在决策周期优先采用在线 Pi 建议：

- Pi 在线且存在新 advice：`last_decision_source = "pi"`
- Pi 超过 30 秒无消息：丢弃 stale advice，回到本地规则
- Pi 建议浇水时仍受 ESP32 温度护栏约束
- 动作时长仍按 ESP32 本地上限裁剪

同时修复一个顺手发现的问题：`action_runtime.py` 原先对补光灯也使用 `PUMP_MAX_RUN_SEC` 裁剪，本次改为 light 动作用 `LIGHT_MAX_RUN_SEC`。

### #37 树莓派 serial_gateway 验收模式

`tools/serial_gateway.py` 保留纯协议层 `GatewayCore`，并增强硬件运行层：

- `--auto-advice`：收到 ESP32 `report` 后用保守规则生成建议
- `--test-advice water --test-duration 8`：收到第一条 report 后只下发一次固定 advice，方便验收“树莓派能让 ESP32 浇水”
- 持续发送 `ping`，ESP32 自动回 `pong`
- 继续把 ESP32 `report` 转成 dashboard `/api/state`

### #38 UART 实机初始化与内存顺序修正

接好 ESP32 GPIO17/16 与树莓派 GPIO15/14/GND 后，开始实机验证 UART。排查过程：

1. 设备端原本没有 `UART_ENABLED`，仅上传了 `uart_link.py`，默认不会启动 UART。
2. 打开 `UART_ENABLED=True` 后，UART 放在 OLED 初始化之后会失败：

```text
E uart: UART driver malloc error
[UART] init skipped: [Errno 1] EPERM: ESP_FAIL
```

3. 把 UART 提前到 WiFi 之后、OLED 之前，UART 可以初始化，但随后 OLED/I2C 初始化触发 MicroPython 底层崩溃并重启。
4. 结论：ESP32 同时启用 WiFi 驱动、UART2、OLED/I2C 时堆内存压力过高，双层架构应把联网职责移到树莓派。
5. 增加 `UART_SKIP_WIFI=True`，UART 模式下 ESP32 跳过 WiFi，再先初始化 UART2，随后初始化 OLED。实机日志确认稳定：

```text
[WiFi] Skipped: UART mode uses Raspberry Pi networking
[UART] Pi link initialized
[Display] OLED initialized successfully
[Init] Starting plant selection...
```

当前 ESP32 侧状态：

- UART2 初始化成功
- OLED 正常
- 开机植物选择和天数选择恢复
- ESP32 WiFi 被跳过是预期行为
- 树莓派端 `/dev/serial0` report/ping/pong/advice 全链路仍需继续验收

## 验证

```powershell
py -m pytest
```

结果：`144 passed`。

覆盖点：

- ESP32/Pi 双端 JSON-over-Line 编解码兼容
- Pi ping → ESP32 pong → Pi 识别 pong
- Pi advice → ESP32 decision 转换
- 主循环 UART poll/report 注入点
- Pi advice 在线优先、离线丢弃
- 补光灯时长使用 `LIGHT_MAX_RUN_SEC`
- UART 模式下可关闭 ESP32 WiFi 管理，由树莓派承担联网职责

## 下一步

- 树莓派启用 `/dev/serial0`：关闭 serial login shell，开启 serial hardware
- 树莓派运行 `python3 tools/serial_gateway.py --port /dev/serial0 --baud 115200 --auto-advice`
- 完成 report/ping/pong/advice 全链路验收
- 在树莓派上同时运行 `dashboard_server.py`，让大屏接管 ESP32 原 WiFi 遥测职责
- 阶段三再把云端 AI 调用迁到树莓派侧，ESP32 只保留本地规则和执行

## 续：树莓派端实机验收完成（同日晚些，远程操作 mx@192.168.125.121）

从 Windows 开发机 SSH 进树莓派（免密 SSH + 免密 sudo），把 `/dev/serial0` 全链路跑通。期间踩并清掉四个坑：

### #39 串口启用与 mini-UART 控制台争用

1. `serial_gateway.py` 当时没同步到 Pi（Pi 上 `/home/mx/spacefarm` 是手动 scp，不是 git）→ 补传。
2. **共地松动**：网关只读到持续 `0xFF/0xFE` 噪声（0% 可打印）。正常 UART 空闲线应安静（0 字节），持续噪声＝RX 悬空/未共地。重插 ESP32 GND↔Pi GND 后立刻读到干净 report（`plant:生菜` UTF-8 正确解码）。
3. **mini-UART 兼内核控制台争用**（关键）：`/dev/serial0`→`ttyS0`（mini-UART），`cmdline.txt` 里 `console=serial0,115200` 把它当内核控制台。现象：**只读嗅探能拿到干净 report，但网关一旦回写 ping/advice，RX 立刻死**；且 ttyS0 是 root-only 600，要 sudo。修法：`sed -i 's/console=serial0,115200 //' /boot/firmware/cmdline.txt`（保留 `console=tty1`）+ 重启。重启后 ttyS0 变 `root:dialout 660`（mx 免 sudo 可开）、getty inactive+disabled。双向闭环跑通，后续 report `ai_src` 变 `pi`。备份 `cmdline.txt.bak-spacefarm`。

### #40 开机自启服务

新增 systemd 服务 `spacefarm-gateway.service`（User=mx, Group=dialout, Restart=always），`--auto-advice` + 转发云端 dashboard `http://43.156.68.157:8790/api/state`，已验证云端 `live:true` 实时刷新。

### #41 openclaw 硬件看门狗 `board` 误杀（含为何引入 SPACEFARM_DASHBOARD）

这台 Pi 跑着 openclaw 智能体沙箱，用户级服务 `hw-watchdog.service`（`/home/mx/pi-project/deploy/scripts/hardware_watchdog.sh`）每 30s `kill -9` 命令行匹配 `python.*(rpi_ws281x|RPi.GPIO|neopixel|board|adafruit|...)` 的进程，意在拦截绕过 hardware_api 直接 `import board` 碰 GPIO 的脚本。**裸子串 `board` 误伤了 `--dashboard` 和用户自己的 `dashboard_server.py`**，把网关每 27s 杀一次。

两处修复：

- **网关侧**：`serial_gateway.py` 的 `--dashboard` 默认改读环境变量 `SPACEFARM_DASHBOARD`，systemd 用 `Environment=` 传 URL，命令行不再含 `board`（看门狗 `ps aux` 只看 argv）。详见该参数上方注释。
- **看门狗侧**：把裸 `board` 收紧为**无反斜杠**词边界 `(^|[^a-zA-Z0-9_])board([^a-zA-Z0-9_]|$)`。**踩坑提醒**：不要用 `\b`——经 bash heredoc/scp 传输会被折成单反斜杠、Python 解成退格符 0x08，正则静默失效；字符类最稳。重启用 `XDG_RUNTIME_DIR=/run/user/1000 systemctl --user restart hw-watchdog.service`。备份 `hardware_watchdog.sh.bak-spacefarm`。实测 7 条用例：`dashboard_server.py`/`--dashboard`/`keyboard` 不再误杀，真 `import board`/`board.D18`/`RPi.GPIO` 仍拦。

收尾状态：ESP32 飞控 ─UART→ Pi 网关（systemd 常驻）─HTTP→ 云端大屏，全链路跑通且开机自启。步骤4 浇水 advice 已下发，泵物理出水待现场目视确认。

### #42 砍掉单层老路 + DeepSeek 搬到树莓派（分支 refactor/remove-wifi-only-path）

决定：项目收敛为**唯一**的双层架构，ESP32 不再联网/直传遥测/直连云 AI；DeepSeek 从 ESP32 迁到树莓派网关。动机是消除双模式分支带来的"乱"，并把 TLS/内存压力大的云 AI 调用放到联网稳、内存大的 Pi 上。

固件侧（Chunk 1，净删 ~1658 行）：

- 删模块：`wifi_client.py`、`telemetry.py`、`ai_client.py`、`diagnostics/wifi_*.py`、`mem_profile.py`
- `main.py`/`boot_runtime.py`/`loop_runtime.py`：去 WiFi 连接/重连、`send_telemetry`、`manage_wifi`、`_uart_skip_wifi` 等双模式分支；UART 由"可选"变"唯一上下行通道"
- `decision.py`：删 `_should_request_ai` 等云 AI 门控，只留 `local_fallback_decision` 本地规则兜底
- `config.py.example`：删 `WIFI_*`/`AI_*`/`DASHBOARD_*`/`TELEMETRY_*` 与 `UART_ENABLED`/`UART_SKIP_WIFI` 开关
- 测试：删 `test_ai_parse.py`；裁 `test_runtime_edges`（AI 占位/门控/telemetry 三类）、`test_loop_runtime`（wifi/重连）

树莓派侧（Chunk 2，AI 搬家）：

- 新增 `tools/pi_advisor.py`：移植 `ai_client.SYSTEM_PROMPT` 的 prompt 构造 + `ai_proxy.request_decision` 的 DeepSeek 调用，纯 stdlib、HTTP 依赖注入可测；key/model 走 `SPACEFARM_AI_*` 环境变量（不进 argv，规避看门狗、key 不进 `ps`）
- `serial_gateway.py` 新增 `--ai-advice`：收 `report` → 调 DeepSeek → `advice` 回 ESP32；AI 失败自动回退 `--auto-advice` 启发式
- 新增 `tests/test_pi_advisor.py`：11 用例（prompt/校验/code-fence/HTTP 注入/降级/信号白名单跨端一致）

**三层降级链**就此完整：① DeepSeek（Pi，最聪明）→ ② `--auto-advice` 阈值规则（Pi）→ ③ ESP32 本地规则（板上，永远在）。

测试：**133 passed**（原 144 − 22 老路测试 + 11 advisor 测试）。

待办：① DeepSeek 的 `SPACEFARM_AI_API_KEY` 要配到 Pi 的 systemd 服务；② 实机验证 `--ai-advice` 往返（卡在地线）；③ 合并分支到 master 前确认硬件链路稳定（砍老路后无 WiFi 兜底）。
