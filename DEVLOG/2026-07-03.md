# 2026-07-03 — 云端视觉同步与写接口加固

- 新增 Pi `spacefarm-vision-sync.service`：SQLite outbox、租约、指数退避、每轮单事件限速补传。
- 云端新增 `POST /api/vision/status`、`POST /api/vision/events`、`PUT/GET /api/vision/images/{event_id}` 和事件列表接口。
- JPEG 与 JSON 分离限额；校验事件 ID、JPEG 签名、字节数和 SHA-256；图片使用 ETag 与 immutable 缓存。
- 云端视觉元数据和图片持久化，Dashboard 重启后可继续展示。
- 筛选写接口改收 `screening.input.v1` 原始逐周期候选/对照数据，由服务端复算 ΔControl、中位数、IQR、独立周期数、表型分和证据等级。
- 遥测、视觉上传、实验设置使用三个独立私有令牌；无令牌仅允许 loopback 写入；生产 CORS 改为精确 origin。
- 腾讯云、树莓派已部署；公网大屏实测显示 `WAITING_TELEMETRY`，不再把“没有新遥测”误报为视觉服务离线。
- 端到端 smoke 事件完成 JPEG→事件→latest→ETag 验收后已清除，未混入正式实验数据。
- 全量测试：199 passed。

## 本机运维与人工复核补齐

- 图片使用固定 `event_id` URL，不再给 JPEG 追加时间戳；JSON 轮询继续 `no-store`。
- 清理多模态 Worker 未使用的返回字节变量；拍摄节奏字段统一为 `last_accepted_capture_at`，分析成功时间为 `last_analysis_success_at`。
- 新增 loopback `spacefarm-vision-local-api.service`：`/healthz`、人工拍摄请求、事件列表、预览图和人工标签接口。
- capture/api/cloud 三个服务写独立心跳；健康检查汇总调用额度、磁盘水位和最近拍摄/分析时间。
- 人工拍摄只绕过两小时间隔，不绕过遥测、光线和质量门；人工标签追加留痕，不覆盖AI结果，并触发事件重新同步云端。
